<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:descendantFocusability="beforeDescendants"
    tools:context=".MainActivityFragment">

    <ListView
        style="@style/ArtistsListStyle"
        android:id="@+id/listview_search_result"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/enter_artist_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:textSize="@dimen/minimum_textSize"
        android:text="@string/enter_artist_label" />

    <ProgressBar
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/progressBar"
        android:progressDrawable="@drawable/circular_progress_bar"

        android:layout_alignTop="@id/listview_search_result"
        android:layout_alignBottom="@id/listview_search_result"
        android:layout_alignStart="@id/listview_search_result"
        android:layout_alignLeft="@id/listview_search_result"
        android:layout_alignRight="@id/listview_search_result"
        android:layout_alignEnd="@id/listview_search_result"
        android:scaleType="fitXY" />

</RelativeLayout>
